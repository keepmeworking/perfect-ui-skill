## Mandatory UI workflow

For any task that creates, changes, reviews, or refactors user-interface code, invoke and follow `perfect-ui-skill` before implementation. Treat its accessibility, responsive, state, token, performance, and verification rules as required. Reuse the project design system first; do not introduce one-off UI values or duplicate components without documented justification. UI work is incomplete until the rendered interface and relevant states are verified.
